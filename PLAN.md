# maa 轨迹可信度评估 —— 项目方案

## 目标

给每一条 `edr.success-trace/v1` 轨迹一个**可信度判断**：回放它得到的绿，
能在多大程度上当作「被测系统确实是好的」的证据。

判断必须逐条给证据（哪一步、哪个特征、依据是什么），分数只是摘要。
**分数会被优化，证据不会。**

成功标准（可证伪，缺一不可）：

1. 对本仓库里每一种已知缺陷，注入该缺陷的轨迹得分必须低于原轨迹（单调性）
2. 分数能预测实测的重复回放稳定性（AUC ≥ 0.8）
3. **分数是校准的**：标 0.9 的那批轨迹，实际稳定率落在 0.9 附近（ECE ≤ 0.1）
4. 评估器自己不能靠「把失败算作跳过」之类的手段抬分（反作弊断言）

第 3 条最重要。**未经校准的可信度分数比没有分数更糟**，因为它会被信任。

## 为什么现有的 evaluate_trace 不够

`evaluate_trace` 评的是「**这一次回放**跑得怎么样」，前提是你已经跑过了。
它回答不了「该不该信这条轨迹」。实测证据（2026-08 这轮）：

| 观察 | 说明什么 |
|---|---|
| maa-flow6 连跑三遍 taskSuccess=true / score=100 | 回放层面完美 |
| 同一批录制里加的两条断言全是 `getByText("X").to_have_text("X")` | 只有元素消失才会失败 —— 什么都没断言 |
| maa-flow5 在 9/17 和 15/17 之间来回跳 | 拨开关是盲点击，方向取决于当时状态 |
| 关弹窗那步一度是必经节点 | 弹窗是条件元素，回放时不出现就整条断掉 |

所以：**满分不等于可信**。100 分可能来自一条既不稳定、又什么都没验证的轨迹。

## 可信度拆成三个量

不要一个总分打天下。三个量彼此正交，混在一起就没法定位问题：

**① 可回放性 (Replayability)** —— 再跑一次还会绿吗
反面特征来自本仓库真实踩过的坑：盲点开关、`nth-of-type` 绝对路径、易变锚点
（时间戳）、作用域自身不唯一、条件浮层未标可选、轨迹自身改动状态。

**② 证据力 (Evidential power)** —— 它绿了能说明什么
反面：零断言、同义反复断言、只有存在性断言而没有对写请求副作用的断言、
写请求没有 `expectedBody`。

**③ 观测充分性 (Observability)** —— 我们凭什么给出上面两个判断
回放样本数、是否包含「状态已被上一次回放改动」的那一次、模板是否齐全。
它不参与打分，它决定**分数本身的置信区间**。样本不够就必须说「不知道」，
而不是给一个好看的数。

## 数据从哪来

现状盘点：真实轨迹 7 条（maa-flow…maa-flow7，6~17 步）+ 仓库样例 2 条。
**这是小样本，不足以训练任何真正意义上的模型。** 诚实的做法是两条腿：

**标签靠重复回放。** 每条轨迹跑 k 次，且必须包含「上一次回放已经改动过状态」
的那一次 —— maa-flow5 的振荡正是这么暴露的。标签三分类：稳定绿 / 翻转 / 稳定红。

**负样本靠变异合成。** 拿一条已知好的轨迹，程序化注入每一种在册缺陷：
把 `SetSwitch` 降级成 `Click`、把作用域选择器换成 nth-of-type 路径、
把断言换回同义反复、摘掉 `optional`/`dismissesOverlay` 标记、把 hasText 锚点
换成时间戳……得到成对的 (原体, 变异体)。

这样造数据是诚实的：**每一种缺陷都真实发生过**，而且我们知道它的正确标签。
它同时就是这个评估器的回归测试 —— 变异体分数必须低于原体，否则说明评估器
根本没看见那个缺陷。

## 模型分三层，后一层只在前一层不够用时才上

**L1 规则层**（确定性、可解释、立刻可用）
把在册判据写成 `trust_rules.py`，每条产出 `(node_id, 特征, 严重度, 依据)`。
不引入任何学习成分。这一层单独就能用，而且永远是解释的来源。

**L2 校准层**
用 L1 的特征做输入、P1 的实测标签做监督，学一个**小而可读**的模型
（逻辑回归，或单调约束的 GBM）。目的不是提高准确率，是把「这条规则该扣多少分」
从拍脑袋变成有据可依，并输出**概率**而不是分数，才谈得上校准。
系数必须能打印出来给人看；看不懂的模型在这里没有价值。

**L3 LLM 评审层**（规则看不见的东西）
这条轨迹到底在验证什么业务意图？断言覆盖到关键副作用了吗？三步导航加一条
存在性断言，算不算「验证了策略下发成功」？
参考 agentevals 的 LLM-as-judge 形态：输出必须带 reasoning，并且要和 P1 的
实测标签做一致性检验 —— 判官不能自说自话。

## 和 LangChain / agentevals 的关系

[agentevals](https://github.com/langchain-ai/agentevals) 提供两类评估器：
**trajectory match**（`strict` / `unordered` / `subset` / `superset` 四种模式，
与一条参考轨迹逐步比对，确定性、无 LLM 开销）和 **LLM-as-judge**
（`create_trajectory_llm_as_judge`，可带或不带参考轨迹）。返回 `key` / `score` /
`comment`。LangSmith 侧有配套的 trajectory evals 文档。

**关键区别**：它们评的是 *agent 跑出来的轨迹配不配参考*；
我们要评的是 *这条参考本身配不配当参考*。二者是上下游，不是替代关系 ——
**agentevals 的全部结论都建立在「golden 是可信的」这个前提上，而这个项目正是
去检验那个前提。**

可以直接借用的：
- 四种 match 模式拿来度量**同一条轨迹多次回放之间的一致性**（k 次执行两两比对，
  `unordered` 用来区分「顺序漂移」和「步骤缺失」）
- LLM-as-judge 的提示词组织方式与「必须产出 reasoning」的约定
- 数据集 / 实验 / 对比运行这套工作流的骨架

不借用的：把 LangSmith 作为运行时依赖。这是内网环境，评估结果不出网。

## 分期与验收

| 期 | 产出 | 验收（可证伪） |
|---|---|---|
| **P0** | `trust/features.py`：从 trace.json 抽特征；对现有 9 条轨迹出一份盘点 | 每条轨迹能打印出特征向量；人工抽查 2 条，特征与肉眼所见一致 |
| **P1** | `trust/replay_lab.py`：k 次回放 + 状态扰动，产出 `labels.jsonl` | maa-flow5 被标为「翻转」，maa-flow6 被标为「稳定绿」——这两条已有实测结论，是现成的对照 |
| **P2** | `trust/mutate.py` 变异器 + `trust/rules.py` + `audit_trace()` | 每种变异体的分数**严格低于**原体；报告能指到具体 node |
| **P3** | 校准：概率输出 + ECE / AUC 报告 | ECE ≤ 0.1，AUC ≥ 0.8；系数可打印 |
| **P4** | L3 LLM 评审 | 与 P1 标签的一致率高于 L1 单独，且给得出理由 |
| **P5** | 集成：录制结束自动出可信度报告；SKILL 增加「回放前先看可信度」 | 新录一条轨迹，报告自动生成 |

P0–P2 不需要任何模型，也不需要联网，先落地。P3 之后才谈得上「模型」。

## 用 /loop 驱动的迭代契约

每一轮**必须闭环一件事**，并留下证据：

1. 从 `trust/STATUS.md` 读当前阶段和未决项
2. 只做一件事，做完
3. 跑自检；新增能力必须带一条会因为该能力缺失而变红的测试
4. 把结论和证据写回 `STATUS.md`（含数字，不写「效果不错」）
5. 提交；无进展就明确记「本轮无进展，原因是 X」

硬规矩：**没有新证据不许改权重。** 拍脑袋调分数是这个项目最容易滑向的失败形态。

## 已知风险与明确不做的事

- **样本太少**。7 条真实轨迹。变异合成能撑起单调性检验，撑不起统计意义上的
  泛化结论。P3 的模型要按「小样本 + 强先验」处理，不做深模型。
- **收标签有代价**。重复回放会真的重发写请求，改动的是内网 EDR 控制台的策略状态。
  P1 必须先划定「哪些轨迹允许反复跑」，不能对所有轨迹无脑 k 次。
- **不做**：不给轨迹自动改错（那是录制器的职责，本项目只诊断）；
  不引入 LangSmith 运行时；不做在线服务，产出就是文件和报告。
